---
import SlideLayout from '../../layouts/slide.astro';
import AnimatedContent from '../../components/AnimatedContent';
import { Card, CardContent } from '@/components/ui/card';
import ComparisonTable from '../../components/ComparisonTable';

// Slide content data
const slideData = {
  title: "Solution Approach Comparison",
  pageNumber: 6,
  totalPages: 19,
  comparisonItems: {
    "User Experience": {
      msStack: ["Microsoft Teams interface", "Copilot for user interactions", "SharePoint for documents"],
      n8nStack: ["Microsoft Teams interface", "Copilot as user-facing assistant", "SharePoint for knowledge base"]
    },
    "Technical Implementation": {
      msStack: ["IT team manages all components", "Power Automate for workflows", "Limited customization options"],
      n8nStack: ["Umbral manages technical components", "N8N for heavy lifting (invisible to users)", "Extensive customization without user burden"]
    },
    "Integration Capabilities": {
      msStack: ["Microsoft ecosystem focus", "Limited third-party options", "Licensing for each connector"],
      n8nStack: ["Full Microsoft ecosystem compatibility", "300+ pre-built integrations", "No additional connector licensing"]
    },
    "Maintenance Requirements": {
      msStack: ["HG Fenton team maintains all components", "Regular updates needed", "Technical expertise required"],
      n8nStack: ["Umbral handles all technical maintenance", "Automatic updates", "No technical burden on HG Fenton team"]
    }
  },
  prevPage: "/btcs/page-5",
  nextPage: "/btcs/page-7"
};
---

<SlideLayout 
  title={slideData.title}
  pageNumber={slideData.pageNumber}
  totalPages={slideData.totalPages}
>
  <div class="space-y-8">
    <AnimatedContent client:load>
      <h1 class="text-4xl font-bold mb-6">{slideData.title}</h1>
      
      <ComparisonTable 
        client:load
        title="Microsoft Interface vs. Technical Implementation"
        items={slideData.comparisonItems}
      />
    </AnimatedContent>
    
    <AnimatedContent client:load delay={0.4}>
      <div class="mt-6 p-4 bg-muted rounded-lg">
        <h3 class="font-semibold mb-2">Hybrid Approach Benefits</h3>
        <p class="mb-3">
          The N8N solution offers a unique hybrid approach: your team continues to use familiar Microsoft tools while the technical complexity is managed behind the scenes.
        </p>
        <ul class="list-disc pl-5 space-y-1">
          <li><strong>Same Microsoft Interface:</strong> Users interact with Teams, Copilot, and SharePoint just as they would in the Microsoft-only approach</li>
          <li><strong>Hidden Technical Complexity:</strong> N8N handles ingestion and automation workflows without requiring your team to manage them</li>
          <li><strong>Best of Both Worlds:</strong> Microsoft's familiar interface with N8N's powerful automation capabilities</li>
          <li><strong>Reduced Technical Burden:</strong> Your team focuses on business outcomes, not technical implementation</li>
        </ul>
      </div>
    </AnimatedContent>
  </div>
</SlideLayout>
