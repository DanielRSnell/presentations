---
import SlideLayout from '../../layouts/slide.astro';
import AnimatedContent from '../../components/AnimatedContent';
import { Card, CardContent } from '@/components/ui/card';

// Slide content data
const slideData = {
  title: "Implementation Timeline",
  pageNumber: 13,
  totalPages: 19,
  msStackTimeline: [
    { 
      phase: "Phase 1 - Foundational Development", 
      duration: "45 days", 
      activities: [
        "Export and analyze historical ticket data",
        "Build comprehensive knowledge base",
        "Develop chatbot using Microsoft Copilot Studio",
        "Implement Power Automate workflows",
        "Microsoft Teams integration",
        "Training and documentation",
        "Sequential Knowledge Base Search"
      ] 
    },
    { 
      phase: "Phase 2 - Advanced Automation", 
      duration: "30 days", 
      activities: [
        "FreshService API integration",
        "Real-time ticket processing",
        "Enhanced automation capabilities",
        "Performance monitoring tools",
        "System optimization"
      ] 
    },
    { 
      phase: "Phase 3 - Add-ons / Reporting", 
      duration: "15 days", 
      activities: [
        "Confidence Score Refinement",
        "Reporting"
      ] 
    }
  ],
  n8nTimeline: [
    { 
      phase: "Phase 1 - Foundational Development", 
      duration: "50 days", 
      activities: [
        "Export and analyze historical ticket data",
        "Build knowledge base from historical ticket data",
        "Implement email-based workflow automation using N8N",
        "Integration with existing systems",
        "Training session for the development team"
      ] 
    },
    { 
      phase: "Phase 2 - Advanced Automation", 
      duration: "40 days", 
      activities: [
        "Direct FreshService API integration",
        "Real-time ticket processing and updates",
        "Enhanced automation capabilities",
        "Advanced monitoring and optimization tools",
        "Continuous improvement framework"
      ] 
    }
  ],
  prevPage: "/btcs/page-12",
  nextPage: "/btcs/page-14"
};
---

<SlideLayout 
  title={slideData.title}
  pageNumber={slideData.pageNumber}
  totalPages={slideData.totalPages}
>
  <div class="space-y-8">
    <AnimatedContent client:load>
      <h1 class="text-4xl font-bold mb-6">{slideData.title}</h1>
      
      <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
        <Card className="w-full">
          <CardContent className="p-6">
            <h3 class="text-xl font-semibold mb-4">Microsoft Stack Timeline <span class="text-sm font-normal text-muted-foreground">(90 days total)</span></h3>
            <div class="space-y-4">
              {slideData.msStackTimeline.map((phase) => (
                <div class="border-l-4 border-blue-500 pl-4 py-2">
                  <h4 class="font-bold">{phase.phase} <span class="font-normal text-muted-foreground">({phase.duration})</span></h4>
                  <ul class="list-disc pl-5 mt-2">
                    {phase.activities.map((activity) => (
                      <li>{activity}</li>
                    ))}
                  </ul>
                </div>
              ))}
            </div>
          </CardContent>
        </Card>
        
        <Card className="w-full">
          <CardContent className="p-6">
            <h3 class="text-xl font-semibold mb-4">N8N Solution Timeline <span class="text-sm font-normal text-muted-foreground">(90 days total)</span></h3>
            <div class="space-y-4">
              {slideData.n8nTimeline.map((phase) => (
                <div class="border-l-4 border-purple-500 pl-4 py-2">
                  <h4 class="font-bold">{phase.phase} <span class="font-normal text-muted-foreground">({phase.duration})</span></h4>
                  <ul class="list-disc pl-5 mt-2">
                    {phase.activities.map((activity) => (
                      <li>{activity}</li>
                    ))}
                  </ul>
                </div>
              ))}
            </div>
          </CardContent>
        </Card>
      </div>
    </AnimatedContent>
    
    <AnimatedContent client:load delay={0.4}>
      <div class="mt-6 p-4 bg-muted rounded-lg">
        <h3 class="font-semibold mb-2">Timeline Comparison</h3>
        <p>
          Both solutions are designed to be completed within a 90-day timeline. The Microsoft Stack implementation 
          includes three distinct phases, with the majority of time allocated to foundational development. 
          The N8N solution is structured in two phases with more time dedicated to advanced automation features. 
          While the overall timeline is identical, the N8N solution offers greater flexibility in resource allocation 
          and potentially earlier access to advanced features.
        </p>
      </div>
    </AnimatedContent>
  </div>
</SlideLayout>
