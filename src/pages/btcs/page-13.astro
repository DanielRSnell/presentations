---
import SlideLayout from '../../layouts/slide.astro';
import AnimatedContent from '../../components/AnimatedContent';
import { Card, CardContent } from '@/components/ui/card';

// Slide content data
const slideData = {
  title: "Implementation Timeline",
  pageNumber: 13,
  totalPages: 19,
  msStackTimeline: [
    { 
      phase: "Phase 1A - Knowledge Base Setup (Days 1-20)", 
      duration: "20 days", 
      activities: [
        "Export and analyze historical ticket data",
        "Configure SharePoint storage for vector embeddings",
        "Set up Microsoft Copilot Studio environment",
        "Develop initial RAG framework with SharePoint"
      ] 
    },
    { 
      phase: "Phase 1B - Workflow Development (Days 21-45)", 
      duration: "25 days", 
      activities: [
        "Implement Power Automate flows for ticket processing",
        "Configure Microsoft Teams integration",
        "Develop user interface components",
        "Training and documentation"
      ] 
    },
    { 
      phase: "Phase 2A - API Integration (Days 46-65)", 
      duration: "20 days", 
      activities: [
        "FreshService API integration",
        "Real-time ticket processing with Copilot",
        "Enhanced RAG capabilities with SharePoint"
      ] 
    },
    { 
      phase: "Phase 2B - Optimization & Reporting (Days 66-90)", 
      duration: "25 days", 
      activities: [
        "Performance monitoring implementation",
        "System optimization",
        "Confidence score refinement",
        "Reporting dashboard development"
      ] 
    }
  ],
  n8nTimeline: [
    { 
      phase: "Phase 1A - Backend Infrastructure (Days 1-15)", 
      duration: "15 days", 
      activities: [
        "Set up Azure VPS environment (managed by Umbral)",
        "Configure N8N instance for heavy lifting tasks",
        "Establish secure database connections",
        "Set up monitoring tools (invisible to end users)"
      ] 
    },
    { 
      phase: "Phase 1B - Microsoft Integration (Days 16-35)", 
      duration: "20 days", 
      activities: [
        "Connect N8N with Microsoft Teams for seamless user experience",
        "Implement SharePoint integration for document storage",
        "Configure Copilot as user-facing interface and router",
        "Ensure all technical complexity is hidden from HG Fenton team"
      ] 
    },
    { 
      phase: "Phase 1C - Workflow Automation (Days 36-50)", 
      duration: "15 days", 
      activities: [
        "Build email processing workflows (managed by Umbral)",
        "Implement existing systems integration",
        "Develop user training for Microsoft interface only",
        "Initial deployment with Microsoft-focused user experience"
      ] 
    },
    { 
      phase: "Phase 2 - Advanced Features (Days 51-90)", 
      duration: "40 days", 
      activities: [
        "Enhance FreshService integration (technical aspects handled by Umbral)",
        "Implement real-time ticket processing via familiar Microsoft interface",
        "Optimize backend performance (invisible to end users)",
        "Develop reporting dashboards in Microsoft Power BI",
        "Continuous improvement with minimal disruption to users"
      ] 
    }
  ],
  prevPage: "/btcs/page-12",
  nextPage: "/btcs/page-14"
};
---

<SlideLayout 
  title={slideData.title}
  pageNumber={slideData.pageNumber}
  totalPages={slideData.totalPages}
>
  <div class="space-y-8">
    <AnimatedContent client:load>
      <h1 class="text-4xl font-bold mb-6">{slideData.title}</h1>
      
      <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
        <Card className="w-full">
          <CardContent className="p-6">
            <h3 class="text-xl font-semibold mb-4">Microsoft-Only Approach <span class="text-sm font-normal text-muted-foreground">(90 days total)</span></h3>
            <div class="space-y-4">
              {slideData.msStackTimeline.map((phase) => (
                <div class="border-l-4 border-blue-500 pl-4 py-2">
                  <h4 class="font-bold">{phase.phase} <span class="font-normal text-muted-foreground">({phase.duration})</span></h4>
                  <ul class="list-disc pl-5 mt-2">
                    {phase.activities.map((activity) => (
                      <li>{activity}</li>
                    ))}
                  </ul>
                </div>
              ))}
            </div>
          </CardContent>
        </Card>
        
        <Card className="w-full">
          <CardContent className="p-6">
            <h3 class="text-xl font-semibold mb-4">Hybrid Microsoft + N8N Approach <span class="text-sm font-normal text-muted-foreground">(90 days total)</span></h3>
            <div class="space-y-4">
              {slideData.n8nTimeline.map((phase) => (
                <div class="border-l-4 border-purple-500 pl-4 py-2">
                  <h4 class="font-bold">{phase.phase} <span class="font-normal text-muted-foreground">({phase.duration})</span></h4>
                  <ul class="list-disc pl-5 mt-2">
                    {phase.activities.map((activity) => (
                      <li>{activity}</li>
                    ))}
                  </ul>
                </div>
              ))}
            </div>
          </CardContent>
        </Card>
      </div>
    </AnimatedContent>
    
    <AnimatedContent client:load delay={0.4}>
      <div class="mt-6 p-4 bg-muted rounded-lg">
        <h3 class="font-semibold mb-2">Key Advantages of Hybrid Approach</h3>
        <p class="mb-3">
          The hybrid approach leverages the best of both worlds: Microsoft's familiar user interface with N8N's powerful backend automation.
        </p>
        <ul class="list-disc pl-5 space-y-1">
          <li><strong>Familiar User Experience:</strong> HG Fenton team interacts exclusively with Microsoft Teams and Copilot</li>
          <li><strong>Technical Complexity Hidden:</strong> All N8N workflows and database operations are managed by Umbral</li>
          <li><strong>Microsoft Ecosystem Integration:</strong> Seamless connection with SharePoint, Teams, and other Microsoft tools</li>
          <li><strong>Enhanced Automation:</strong> N8N handles heavy lifting tasks behind the scenes without user involvement</li>
        </ul>
      </div>
    </AnimatedContent>
  </div>
</SlideLayout>
