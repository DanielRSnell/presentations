---
import SlideLayout from '../../layouts/slide.astro';
import AnimatedContent from '../../components/AnimatedContent';
import { Card, CardContent } from '@/components/ui/card';

// Slide content data
const slideData = {
  title: "Recommendation & Next Steps",
  pageNumber: 17,
  totalPages: 19,
  msStackAdvantages: [
    "Familiar Microsoft ecosystem for end users",
    "Low-code development for IT team",
    "Managed updates through Microsoft",
    "Single vendor relationship"
  ],
  hybridAdvantages: [
    "Microsoft interface for users (Teams, Copilot, SharePoint)",
    "N8N handles technical implementation (managed by Umbral)",
    "No technical burden on HG Fenton team",
    "Lower costs with same Microsoft compatibility",
    "More powerful automation capabilities"
  ],
  recommendation: "We recommend the hybrid approach that leverages Microsoft's familiar user interface while using N8N for technical implementation and heavy lifting tasks. This gives HG Fenton the best of both worlds: the Microsoft ecosystem your team already knows, with powerful automation that happens behind the scenes without requiring your team to manage it.",
  nextSteps: [
    "Conduct a detailed requirements workshop",
    "Map existing Microsoft tools to hybrid solution",
    "Develop proof-of-concept with Microsoft interface",
    "Create implementation roadmap with minimal disruption to users"
  ],
  prevPage: "/btcs/page-15",
  nextPage: "/btcs/page-17"
};
---

<SlideLayout 
  title={slideData.title}
  pageNumber={slideData.pageNumber}
  totalPages={slideData.totalPages}
>
  <div class="space-y-8">
    <AnimatedContent client:load>
      <h1 class="text-4xl font-bold mb-6">{slideData.title}</h1>
      
      <div class="grid grid-cols-1 md:grid-cols-2 gap-6 mb-6">
        <Card className="w-full">
          <CardContent className="p-6">
            <h3 class="text-xl font-semibold mb-4">Microsoft-Only Approach</h3>
            <ul class="space-y-2 list-disc pl-5">
              {slideData.msStackAdvantages.map((advantage) => (
                <li>{advantage}</li>
              ))}
            </ul>
            <div class="mt-4 p-3 bg-muted rounded-lg">
              <p class="text-sm text-muted-foreground">
                <strong>Key Challenge:</strong> Your team must manage all technical aspects
              </p>
            </div>
          </CardContent>
        </Card>
        
        <Card className="w-full">
          <CardContent className="p-6">
            <h3 class="text-xl font-semibold mb-4">Hybrid Microsoft + N8N Approach</h3>
            <ul class="space-y-2 list-disc pl-5">
              {slideData.hybridAdvantages.map((advantage) => (
                <li>{advantage}</li>
              ))}
            </ul>
            <div class="mt-4 p-3 bg-muted rounded-lg">
              <p class="text-sm text-muted-foreground">
                <strong>Key Benefit:</strong> Technical complexity handled by Umbral
              </p>
            </div>
          </CardContent>
        </Card>
      </div>
    </AnimatedContent>
    
    <AnimatedContent client:load delay={0.3}>
      <Card className="w-full">
        <CardContent className="p-6">
          <h3 class="text-xl font-semibold mb-4">Recommendation</h3>
          <p class="mb-6">{slideData.recommendation}</p>
          
          <h3 class="text-xl font-semibold mb-4">Implementation Roadmap</h3>
          <ol class="space-y-2 list-decimal pl-5">
            {slideData.nextSteps.map((step) => (
              <li>{step}</li>
            ))}
          </ol>
          
          <div class="mt-6 p-4 bg-muted rounded-lg">
            <h4 class="font-semibold mb-2">User Experience Focus</h4>
            <p>
              With the hybrid approach, your team will continue using familiar Microsoft tools like Teams and Copilot. 
              All the technical implementation, ingestion, and automation workflows will be handled by N8N behind the scenes, 
              managed by Umbral. This means you get powerful automation without the technical burden.
            </p>
          </div>
        </CardContent>
      </Card>
    </AnimatedContent>
  </div>
</SlideLayout>
